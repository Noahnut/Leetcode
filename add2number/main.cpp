#include "add2num.h"
#include <iostream>
#include <string>
using namespace std;


int main()
{
    string number_one;
    string number_two;
    cin >> number_one >> number_two;
    add2number(number_one, number_two);
    return 0;
}
